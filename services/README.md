# Services

This directory is reserved for optional Python microservices when ML-specific workloads require it.

Current scaffold:
- `ml-audit-service`: FastAPI placeholder service for future fairness model inference and evaluation tasks.
